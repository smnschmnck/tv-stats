---
import { twMerge } from "tailwind-merge";

const { rating, className } = Astro.props as {
  rating: number;
  className?: string;
};
---

<div
  class={twMerge(
    "flex h-10 w-10 items-center justify-center rounded-md bg-zinc-100 font-medium text-white",
    className
  )}
>
  <span>{rating}</span>
</div>
